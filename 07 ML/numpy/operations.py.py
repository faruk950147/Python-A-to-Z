"""
    mathamatical operations
    
    + addition +                mean thats means average      
    - subtraction -            difference thats means how much distance between two numbers
    * multiplication *          product thats means total
    / division /                quotient thats means how much times one number can be divided by another number
    
"""

import numpy as np

data = np.array([1, 2, 3, 4, 5])
print(f"mean of data: {np.mean(data)}")
