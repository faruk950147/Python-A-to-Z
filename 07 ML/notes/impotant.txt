Outlier
Dataset-এর মধ্যে এমন কিছু value যা অন্য সব data point থেকে অনেক দূরে থাকে।

Example:
Salary: 10k, 12k, 11k, 500k
এখানে 500k outlier

Problem: model ভুলভাবে learn করতে পারে

Linear Relationship
যখন দুইটা variable-এর মধ্যে সরল সম্পর্ক থাকে (একটা বাড়লে/কমলে অন্যটাও নিয়মিতভাবে বাড়ে/কমে)।

Example:
Study time বাড়লে marks বাড়ে

Multicollinearity (Feature problem)
যখন একাধিক feature একে অপরের সাথে খুব বেশি সম্পর্কিত (correlated) থাকে।

Example:
Height in cm এবং height in inch

Problem: model বুঝতে পারে না কোন feature বেশি important

Class Imbalance (Classification)
যখন এক class-এর data অনেক বেশি এবং অন্য class-এর data খুব কম থাকে।

Example:
99% Not Fraud, 1% Fraud

Problem: model majority class-ই predict করে

Overlapping Classes
যখন different class-এর data একই জায়গায় overlap করে যায়, আলাদা করা কঠিন হয়।

Example:
Cat vs Dog features কিছু ক্ষেত্রে similar

Problem: decision boundary ঠিকভাবে তৈরি হয় না

Distribution Problem (Diagonal plots)
যখন data-এর distribution skewed বা uneven থাকে। Pairplot-এর diagonal plot দিয়ে এটা দেখা হয়।

Example:
Data সব এক পাশে জমে আছে (right skewed)

Problem: model ঠিকভাবে pattern শিখতে পারে না

=========================================== Checkpoint ===========================================


এই গ্রাফ দেখে যা বোঝা যাচ্ছে:

1. Distribution Problem (আছে)

Diagonal histogram-এ দেখো:

Fare → ডানদিকে লম্বা tail গেছে
SibSp, Parch → বেশিরভাগ মান 0–1 এ জমা

এটার মানে:

Data skewed
Normal distribution না

সমাধান (optional):

df['Fare'] = np.log1p(df['Fare'])
2. Outlier (আছে)

Fare সম্পর্কিত scatter-এ দেখো:

বেশিরভাগ value নিচে
কয়েকটা point অনেক উপরে (২০০–৫০০)

এগুলো outlier হতে পারে।

চেক:

sns.boxplot(df['Fare'])
plt.show()
3. Class Imbalance (মাঝারি)

Survived histogram দেখো:

0 বেশি
1 কম

মানে class পুরো equal না।

চেক:

sns.countplot(x='Survived', data=df)
plt.show()
4. Strong Linear Relationship (খুব বেশি দেখা যাচ্ছে না)

Scatter গুলোতে:

পরিষ্কার সোজা line নাই
মানে feature গুলা খুব strong linear না
5. Multicollinearity (এখনো নিশ্চিত না)

Pairplot দিয়ে একটু বোঝা যায়, কিন্তু Heatmap ভালো।

চালাও:

plt.figure(figsize=(10,6))

sns.heatmap(
    df.corr(),
    annot=True,
    cmap='coolwarm'
)

plt.show()

দেখবে:

0.90+ → সমস্যা
0.70+ → খেয়াল করা লাগবে

তোমার screenshot দেখে আমার verdict:

Problem	Status
Outlier	আছে
Distribution	আছে
Class imbalance	অল্প আছে
Linear relationship	কম
Multicollinearity	Heatmap লাগবে

এখন heatmap-এর screenshot দাও, আমি বলে দেব কোন column problem করছে।