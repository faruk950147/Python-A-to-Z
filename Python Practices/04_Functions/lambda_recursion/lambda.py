# ============================= What is Lambda Function ==============================
# A lambda is a small anonymous function defined with the lambda keyword.
# Syntax: lambda arguments: expression

# ============================= basic lambda ============================================
# Simple examples of lambda with one or more arguments

# ============================= lambda vs normal function ===============================
# Differences between lambda and def function

# ============================= lambda with no arguments ================================
# lambda: expression

# ============================= lambda with single argument ==============================
# lambda x: x + 10

# ============================= lambda with multiple arguments ===========================
# lambda x, y: x + y

# ============================= lambda inside functions =================================
# Using lambda as a local helper function

# ============================= lambda with higher-order functions =======================
# map(), filter(), reduce(), sorted(), key= parameter

# ============================= lambda with conditional expression ======================
# lambda x: "Even" if x % 2 == 0 else "Odd"

# ============================= limitations of lambda ===================================
# - Single expression only
# - Not for complex logic
# - No statements (e.g., return, print)
