# open() function
# syntax: 
# open(filename, mode='r' | 'w' | 'a' | 'x' | 'b' | 't' | 'r+' | 'w+' | 'a+' | 'x+', 
#      buffer, encoding=None, errors=None, newline=None, closefd=True, opener=None)

# file_name = "example.txt"
# mode = "r"
# buffer = 1024
# encoding = "utf-8"
# errors = "strict"
# newline = "\n"
# closefd = True
# opener = None

# buffer is the integer number of bytes to be read at a time set to -1 for default size of file
# encoding is the encoding of the file
# errors is the error handling scheme
# newline is the newline mode
# closefd is whether to close the file descriptor
# opener is a custom opener

