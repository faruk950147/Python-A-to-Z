🔹 Git Local Commands (লোকাল কম্পিউটারে)

প্রথমে Git install করতে হবে। তারপরঃ

# 1. ফোল্ডার initialize করা
git init

# 2. স্ট্যাটাস দেখা (কোন ফাইল add/commit হয়নি)
git status

# 3. ফাইল track করা (stage এ তোলা)
git add filename.txt
# সব ফাইল যোগ করতে
git add .

# 4. ফাইল commit করা (লোকাল history তে save)
git commit -m "First commit"

# 5. commit history দেখা
git log

🔹 GitHub এর সাথে কানেক্ট করা

প্রথমে GitHub এ একটি নতুন repo বানাও।
তারপর লোকাল repo কে GitHub এর সাথে কানেক্ট করোঃ

# 1. Remote (GitHub Repo) যোগ করা
git remote add origin https://github.com/username/repo-name.git

# 2. লোকাল কোড GitHub এ পাঠানো
git push -u origin main


👉 প্রথমবার করলে main বা master branch হতে পারে।

🔹 অন্যদের Repo নামানো (clone করা)
git clone https://github.com/username/repo-name.git

🔹 সাধারণ Workflow

প্রতিদিন কাজ করার নিয়মঃ

# 1. পরিবর্তন হয়েছে কিনা দেখো
git status

# 2. ফাইল stage করো
git add .

# 3. commit করো
git commit -m "কাজের বর্ণনা লিখো"

# 4. GitHub এ push করো
git push origin main

🔹 Update নেওয়া (Pull করা)

অন্য কেউ যদি কোড আপডেট করে থাকে –

git pull origin main