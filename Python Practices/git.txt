ইনস্টল হওয়ার পর টার্মিনালে গিয়ে নাম আর ইমেইল সেট করো (একবার করলেই হবে):

git config --global user.name "তোমার নাম"
git config --global user.email "তোমার ইমেইল"


চেক করার জন্যঃ

git config --list

2. নতুন প্রজেক্ট তৈরি
mkdir myproject
cd myproject
echo "<h1>Hello GitHub</h1>" > index.html
echo "# My First GitHub Project" > README.md

এখন তোমার প্রজেক্টে দুইটা ফাইল আছে – index.html আর README.md

3. Git Initialize করা
git init

এখন এই ফোল্ডারটা Git Repo হয়ে গেলো।

4. স্ট্যাটাস দেখা
git status


দেখাবে কোন ফাইল নতুন আছে, কোনটা add হয়নি।

5. ফাইল Stage করা
git add index.html
git add README.md
# অথবা সব ফাইল একসাথে:
git add .

6. Commit করা
git commit -m "First commit: Added index.html and README.md"


Commit মানে লোকাল সেভ। Git তোমার কাজের snapshot/history রাখবে।

7. GitHub Repo তৈরি

GitHub এ গিয়ে New Repository তৈরি করো।
ধরি নাম দিলা – myproject

এখন Repo এর URL হবে এমনঃ

https://github.com/username/myproject.git

8. লোকাল Repo GitHub এর সাথে কানেক্ট করা
git remote add origin https://github.com/username/myproject.git


এখানে username তোমার GitHub ইউজারনেম হবে।

9. GitHub এ Push করা
git branch -M main      # main branch সেট করা (প্রয়োজনে)
git push -u origin main


এখন তোমার প্রজেক্ট GitHub এ চলে গেলো 🎉

🔄 Daily Workflow (প্রতিদিন কাজ করার নিয়ম)

যখন প্রজেক্টে নতুন পরিবর্তন করবে, তখন করোঃ

git status                  # কি পরিবর্তন হয়েছে দেখো
git add .                   # সব পরিবর্তন stage করো
git commit -m "Update work" # পরিবর্তন commit করো
git push origin main        # GitHub এ পাঠাও

📌 অন্যদের Repo Clone করা
git clone https://github.com/username/repo-name.git


এটা করলে প্রজেক্ট তোমার PC তে ডাউনলোড হয়ে যাবে।

Update নেওয়া (Pull)

অন্য কেউ GitHub Repo তে পরিবর্তন করলে তুমি আপডেট নিতে পারো:

git pull origin main
Extra Commands
git log               # commit history দেখা
git branch            # branch তালিকা
git checkout -b dev   # dev নামে নতুন branch তৈরি
git checkout main     # main branch এ ফেরা
git diff              # পরিবর্তন কী কী হয়েছে তা দেখা



git init → প্রজেক্টে Git চালু করা

git add → ফাইল stage করা

git commit → লোকাল সেভ করা

git remote add origin → GitHub এর সাথে লিঙ্ক করা

git push → GitHub এ পাঠানো

git pull → GitHub থেকে নতুন আপডেট নামানো