lambda কি?
lambda হলো Python-এর একটি anonymous (নামহীন) function, যেটা এক লাইনে লেখা যায়।
আমরা সাধারণত ছোট ছোট কাজের জন্য lambda ফাংশন ব্যবহার করি।

📌 সাধারণ ফর্মেট:
python
Copy
Edit
lambda arguments: expression
arguments = ইনপুট প্যারামিটার

expression = যেটা return হবে (একটি মাত্র line)

উদাহরণ ১: দুই সংখ্যার যোগফল
python
Copy
Edit
add = lambda x, y: x + y
print(add(5, 3))  # Output: 8
এখানে lambda x, y: x + y মানে হলো একটা ছোট function যা x ও y এর যোগফল রিটার্ন করে।

এটা নিচের কোডের সমান:

python
Copy
Edit
def add(x, y):
    return x + y
উদাহরণ ২: sort করার জন্য ব্যবহার
ধরো তোমার একটি লিস্ট আছে যেখানে প্রতিটি আইটেম একজন মানুষের তথ্য:

python
Copy
Edit
people = [['John', 'Doe', '25', 'M'], ['Jane', 'Smith', '22', 'F']]
তুমি যদি তাদের বয়স (index 2) অনুযায়ী sort করতে চাও, তাহলে:

python
Copy
Edit
sorted_people = sorted(people, key=lambda person: int(person[2]))
এখানে lambda person: int(person[2]) মানে:

প্রতিটি person এর মধ্যে থেকে person[2] (যেটা age) নেওয়া হচ্ছে,

সেটাকে int করে sorting এর জন্য return করা হচ্ছে।

 উদাহরণ ৩: শুধু একটি সংখ্যা স্কয়ার করো
python
Copy
Edit
square = lambda x: x * x
print(square(5))  # Output: 25
🆚 lambda vs def
lambda	def
এক লাইনের ফাংশন	এক বা একাধিক লাইনের ফাংশন
নাম ছাড়া তৈরি হয়	def দিয়ে নাম দিতে হয়
সাধারণত ছোট কাজের জন্য	বড় কাজের জন্য উপযুক্ত

উপসংহার:
lambda হলো ছোট ও দ্রুত ফাংশন লেখার উপায়।
যেখানে তুমি def দিয়ে আলাদা ফাংশন লিখে কাজ করো, সেখানে lambda দিয়ে এক লাইনে একই কাজ করা যায়।

